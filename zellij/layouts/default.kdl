layout {

	default_tab_template {
		pane size=2 borderless=true {
			plugin location="file:~/.config/zellij/plugins/zjstatus.wasm" {

				color_fg "#dcdfe1"
				color_bg "#24283b"

				color_black "#1f2335"
				color_red "#f7768e"
				color_green "#9ece6a"
				color_yellow "#e0af68"
				color_orange "#61afef"
				color_magenta "#bb9af7"
				color_cyan "#7dcfff"
				color_white "#faefee"
				color_blue "#B9A88C"

				format_left   "{mode}#[bg=$bg]{tabs}"
				format_center "#[bg=$bg,fg=$fg] Zellij"
				format_right  "{datetime}"
				format_space  "#[bg=$bg]"
				format_hide_on_overlength "true"
				format_precedence "crl"

				border_enabled  "false"
				border_char     "─"
				border_format   "#[fg=#6C7086]{char}"
				border_position "top"

				// hide_frame_for_single_pane "true"

				mode_normal        "#[bg=$blue,fg=$bg,bold] NORMAL #[bg=$bg,fg=$blue]"
				mode_locked        "#[bg=$red,fg=$bg,bold] LOCKED  #[bg=$bg,fg=$red]"
				mode_resize        "#[bg=$green,fg=$bg,bold] RESIZE #[bg=$bg,fg=$green]"
				mode_pane          "#[bg=$blue,fg=$bg,bold] PANE #[bg=$bg,fg=$blue]"
				mode_tab           "#[bg=$yellow,fg=$bg,bold] TAB #[bg=$bg,fg=$yellow]"
				mode_scroll        "#[bg=$blue,fg=$bg,bold] SCROLL #[bg=$bg,fg=$blue]"
				mode_enter_search  "#[bg=$orange,fg=$bg,bold] SEARCH #[bg=$bg,fg=$orange]"
				mode_search        "#[bg=$orange,fg=$bg,bold] SEARCHARCH #[bg=$bg,fg=$orange]"
				mode_rename_tab    "#[bg=$yellow,fg=$bg,bold] RENAME-TAB #[bg=$bg,fg=$yellow]"
				mode_rename_pane   "#[bg=$blue,fg=$bg,bold] RENAME-PANE #[bg=$bg,fg=$blue]"
				mode_session       "#[bg=$blue,fg=$bg,bold] SESSION #[bg=$bg,fg=$blue]"
				mode_move          "#[bg=$blue,fg=$bg,bold] MOVE #[bg=$bg,fg=$blue]"
				mode_prompt        "#[bg=$blue,fg=$bg,bold]  #[bg=$bg,fg=$blue]"
				mode_tmux          "#[bg=$magenta,fg=$bg,bold]  #[bg=$bg,fg=$magenta]"

				// formatting for inactive tabs
				tab_normal              "#[bg=$bg,fg=$bg]#[bg=$bg,fg=$black,bold] {name}{floating_indicator} #[bg=$black,fg=$bg] {index} #[bg=$bg,fg=$black]"
				// tab_normal_fullscreen   "#[bg=$cyan,fg=$bg,bold] {index} #[bg=$bg,fg=$cyan,bold] {name} "
				// tab_normal_sync         "#[bg=$cyan,fg=$bg,bold] {index} #[bg=$bg,fg=$cyan,bold] {name} "

				// formatting for the current active tab
				tab_active              "#[bg=$black,fg=$bg]#[bg=$black,fg=$blue,bold] {name}{floating_indicator} #[bg=$blue,fg=$bg,bold] {index} #[bg=$bg,fg=$blue]"
				// tab_active_fullscreen   "#[bg=$bg,fg=$yellow]#[bg=$yellow,fg=$bg,bold]{index} #[bg=$bg,fg=$yellow,bold] {name}{fullscreen_indicator}#[bg=$bg,fg=$bg,bold] "
				// tab_active_sync         "#[bg=$bg,fg=$yellow]#[bg=$yellow,fg=$bg,bold]{index} #[bg=$bg,fg=$yellow,bold] {name}{sync_indicator}#[bg=$bg,fg=$bg,bold] "

				// separator between the tabs
				tab_separator           "#[bg=$bg] "

				// indicators
				tab_sync_indicator       " "
				tab_fullscreen_indicator " 󰊓"
				tab_floating_indicator   " 󰹙"

				command_git_branch_command     "git rev-parse --abbrev-ref HEAD"
				command_git_branch_format      "#[fg=$blue] {stdout} "
				command_git_branch_interval    "10"
				command_git_branch_rendermode  "static"

				datetime        "#[fg=$fg] {format} "
				datetime_format "%Y-%m-%d %H:%M"
				datetime_timezone "Europe/Paris"
			}
		}

		children

		pane size=2 borderless=true {
			plugin location="zellij:status-bar"
		}

	}

}
